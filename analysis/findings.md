# Findings

Not written yet. This file gets filled in with real output from the queries in `sql/`, once the datasets are connected per `SETUP.md`:

- GA4 funnel conversion rates by stage and month (from `01_ga4_acquisition_funnel.sql`)
- Channel-level conversion and revenue (from `02_ga4_channel_performance.sql`)
- On-time delivery rate by state (from `03_olist_delivery_performance.sql`)
- Delivery delay vs. review score (from `04_olist_delay_vs_reviews.sql`) — this is the number the PRD's problem statement depends on

No placeholder numbers below — this file stays this short until real query output exists to write about.
